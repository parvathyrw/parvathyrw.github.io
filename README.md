# Personal Website

A minimal static site built with [Zola](https://www.getzola.org/).

## Usage

```bash
# Serve locally
zola serve

# Build
zola build
```

## Structure

- `content/` - Markdown content
- `templates/` - HTML templates
- `static/` - CSS and JavaScript
